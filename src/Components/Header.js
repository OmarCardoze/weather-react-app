import React from 'react';

const Header = ({titulo}) => {
    return (
        <div className="title-header">
            <h1>{titulo}</h1>
        </div> 
        
     );
}
 
export default Header;